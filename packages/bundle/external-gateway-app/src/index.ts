/**
 * @deepseek-ai/dsh-external-gateway-app — the static patch bundle for the
 * loopback external-gateway profile. The application behavior belongs to
 * `@deepseek-ai/dsh-external-gateway`; this package only carries the profile
 * patch and its package-owned invariant companion.
 * @module @deepseek-ai/dsh-external-gateway-app
 */

export {}
